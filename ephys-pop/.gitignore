# Course environment / caches
.venv/
__pycache__/
*.pyc
*.egg-info/
.ipynb_checkpoints/
.DS_Store
# The HTML preview is generated (see tools/make_preview.py) and ignored at the
# repo root -- CI renders it for GitHub Pages, so it is never committed.
